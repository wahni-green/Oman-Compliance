[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated (top to bottom).
# The trailing #N on each line is a per-line version marker (bump it to force that line to
# re-run on the next migrate when its underlying field/seed definition changes) — it is not an
# execution-order index across lines, and the two lines below are independent of each other.
execute:from oman_compliance.oman_compliance.setup import create_custom_fields; create_custom_fields() #8
execute:from oman_compliance.oman_compliance.setup import create_designated_zones; create_designated_zones() #1
execute:from oman_compliance.oman_compliance.setup import set_default_settings_currency; set_default_settings_currency() #1